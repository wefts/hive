# Per-stage overrides for SWARM_ENV=prod (NON-secret, committed).
#
# PLACEHOLDER — prod does not exist yet (ADR-14: "does not exist yet — planned
# on k3s"). This file exists now purely so the SWARM_ENV enum is complete and
# `scripts/compose` doesn't need a special case; it is not wired to any running
# substrate. Going public re-plans deployment topology entirely
# (`board/ideas/go-public-deployment.md`) — this file will very likely be
# replaced by that work, not grown in place.
